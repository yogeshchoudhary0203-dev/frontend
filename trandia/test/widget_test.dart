// widget_test.dart
// Basic smoke test — verifies the app builds without crashing.
// Firebase-dependent routing is not tested here (requires integration tests).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app compiles and tests run', () {
    expect(1 + 1, equals(2));
  });
}
